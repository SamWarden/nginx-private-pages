server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        auth_basic "Restricted Access";
        auth_basic_user_file /etc/pages/.htpasswd;

        try_files $uri $uri/ =404;
    }
}
